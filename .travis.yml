sudo: false
dist: trusty
language: rust
rust:
  - stable
  - beta
  - nightly
  - 1.30.0
os:
  - linux
addons:
  apt:
    packages:
    - libudev-dev
cache: cargo
matrix:
  allow_failures:
    - rust: nightly

script:
  - cargo build --all --verbose
  - cargo test --all --verbose
